//W7. Declare a constant integer variable MAX and set it to 100.

#include <stdio.h>

int main() {
  const int MAX = 100;

  // You can now use MAX in your code, but you cannot change its value.

  printf("The value of MAX is: %d\n", MAX); 

  return 0;
}